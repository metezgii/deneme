float x;
float y;
int w=100;
int h=50;



void setup(){
   size(600,600);  
}

void draw(){
  background(0);
  x=width*0.5-w*0.5;
y=height*0.5-h*0.5;

 if (keyPressed==true){
// rect(x,y,w,h);
textSize(128);
   text(key,x,y);
   
   //text("H",x,y);
 } 
}
